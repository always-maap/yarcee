const BASE_URL = 'http://localhost:8082';

// Auth
export const SIGN_UP = `${BASE_URL}/api/sign-up`;
export const SIGN_IN = `${BASE_URL}/api/sign-in`;

// USER
export const USER = `${BASE_URL}/api/user`;

// SANDBOX
export const SANDBOX = `${BASE_URL}/api/sandbox`;
